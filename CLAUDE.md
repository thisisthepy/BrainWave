# CLAUDE.md

`torchnative` 에서 작업하는 에이전트가 반드시 지켜야 할 규정과, 작업에 필요한 컨텍스트입니다.

**이 문서는 `docs/` 전반에서 인용됩니다.** 특히 §5 의 하위 번호(§5.1 · §5.3 · §5.4 · §5.5 · §5.7)는
90 곳 넘게 인용되어 있으므로 **번호를 바꾸지 마십시오.** 내용을 고치는 것은 자유이나, 번호를
재배치하면 그 인용이 전부 다른 곳을 가리키게 됩니다.

---

## 0. 이 프로젝트가 무엇인가

**PyTorch/transformers 생태계를 그대로 두고 `torch._C` 만 네이티브 Rust 확장으로 교체합니다.**
재구현이 아닙니다. 진짜 `transformers` 가 로드되고, 진짜 `from_pretrained` 와 `generate` 가
돌고, 그 밑에서 계산하는 것만 우리 것입니다.

```
rust/torch_c/          `torch._C` 확장 (Rust, pyo3 abi3-py313, candle-core)
  src/aten.rs          연산자 커널. 단일 문(`_aten_dispatch`)으로 모든 op 이 들어온다
  src/bootstrap.py     `include_str!` 로 확장에 구워지는 파이썬 부트스트랩
torchnative/           파이썬 패키지 (quant, export, adapt …)
  src/main/torch/      **upstream 의 vendored 트리. 생성물이고 gitignore 된다. 손대지 마라**
tools/golden/          upstream 과의 값 대조 하네스
tools/wheel/           크로스 빌드와 휠 검증
docs/                  179 개. 각 회차가 무엇을 측정했는지의 기록
```

**단일 문 원칙**: 모든 연산자는 `_aten_dispatch` 를 거칩니다. 우회 경로를 만들지 마십시오.

**`Repr` enum** (`tensor.rs`): `Dense | Quantized | Vulkan | Complex | Meta`. `tensor()` 는
비-`Dense` 에 대해 거절하고, 그 거절이 400 곳 가까운 호출부에 자동으로 퍼집니다. **조용한
폴백을 표현 불가능하게 만드는 것이 이 설계의 목적입니다.** 변형을 늘릴 때 와일드카드
`match` 를 쓰지 마십시오 — 컴파일이 깨지는 것이 좋은 성질입니다.

---

## 1. 커밋 규정

**커밋 메시지와 PR 본문에 `Co-Authored-By: Claude ...` 나 `Generated with Claude Code` 를 절대
넣지 않습니다.** 기본 동작이 붙이도록 되어 있어도 이 규정이 우선합니다.

**서브 에이전트는 커밋하지 않습니다.** `git commit` · `git push` · `git merge` ·
`git checkout -- <path>` 는 조율 세션만 합니다. 에이전트는 `cp` 로 백업하고 작업만 남깁니다.

**`git add -A` 를 쓰지 마십시오.** 파일을 명시하거나, 개수가 보고와 다르면 멈춥니다. 한 번
`git add -A` 로 부분 해결된 병합을 커밋해 **충돌 마커가 그대로 들어간 적이 있습니다.**

---

## 2. 게이트

```sh
PATH="$HOME/.cargo/bin:$PATH" PYTHON=/Volumes/macMini/caches/spike-venv/bin/python \
    bash rust/torch_c/pytests/run.sh
```

Vulkan 테스트까지 돌리려면 (안 주면 이름을 대며 스킵합니다):

```sh
TORCHNATIVE_VULKAN_DYLD="$HOME/Library/Android/sdk/emulator/lib64/vulkan" \
VK_DRIVER_FILES="$HOME/Library/Android/sdk/emulator/lib64/vulkan/libkosmickrisp_icd.json"
```

**기준선 (2026-09-07, `dd2e0a3`)**: 1122 ok / 0 FAIL · DOCWATCH 1011/1011 ·
golden 11420/11420 ops=302 failed=0 pending=0 · `cargo test` 30/30 · golden self-test PASS.

게이트가 도는 것: crate 유닛 테스트 → 파이썬 스위트 전체(43 파일) → golden self-test →
문서 검사기(DOCWATCH, `docs/*.md` + `README.md`).

**파이프로 종료 코드를 읽지 마십시오.** 파일로 리다이렉트하고 `$?` 를 봅니다.

```sh
bash rust/torch_c/pytests/run.sh > /tmp/gate.log 2>&1; echo "EXIT=$?"
```

배경 실행에서는 `echo` 로 끝내면 **스크립트의 종료 코드가 `echo` 의 0** 이 됩니다. 로그의
`EXIT=` 줄을 직접 읽으십시오.

**`_aten_implemented()` 에 op 을 추가하면 `tools/golden/cases.py` 에 케이스 빌더도 추가해야
합니다.** 안 하면 `golden_cases_failed eq 0` 마커가 게이트를 빨갛게 만듭니다. 그 마커는
**실패한 골든 케이스가 세 커밋 동안 초록으로 지나간 뒤에** 생겼습니다 — *passed* 와 *total*
에 `ge` 하한만 있으면 `passed < total` 을 볼 수 없습니다.

**DOCWATCH 마커는 `ge` 를 씁니다.** 공유 전역 카운트에 `eq` 를 쓰면 다음 회차가 정당하게
숫자를 올렸을 때 빨개집니다. 예외는 **0 이어야 하고 0 으로 남아야 하는 두 개**뿐입니다 —
`golden_pending` 과 `golden_cases_failed`. 이들에게는 `ge` 가 무용지물입니다(`failed ge 0` 은
모든 수에 대해 참). `test_release.py` 가 이 규칙을 강제합니다.

---

## 3. 건드리면 안 되는 것

**`torchnative/src/main/torch/`** — upstream 의 vendored 트리입니다. gitignore 되어 있고
`vendor/vendor_torch.sh` 가 생성하며 **조용히 지워집니다.** 손으로 고치지 마십시오.
`__init__.py` 가 없으면 트리가 안 지어진 것입니다:

```sh
sh vendor/vendor_torch.sh && bash vendor/install_shim.sh
```

**이걸 확인하지 않아 한 회차가 몇 시간을 버렸습니다.** 트리가 비면 모든 프로브가 조용히
**upstream torch 를 import** 하고, "export 가 이미 완벽히 동작한다"는 보고가 나옵니다.
그러므로 **모든 프로브에서 다음을 단언하십시오**:

```python
assert hasattr(torch._C, "_aten_implemented")   # shim 인가 upstream 인가
```

**`rust/torch_c/src/bootstrap.py`** 는 빌드 타임에 `include_str!` 로 확장에 구워집니다.
**고치고 리빌드하지 않으면 옛 바이너리를 다시 시험합니다.**

**`/Volumes/macMini/caches/spike-venv`** 에 설치하지 마십시오 (transformers 5.15.1 로 고정).
새 패키지가 필요하면 외장 SSD 에 별도 venv 를 만드십시오.

**`/Volumes/macMini/caches/emsdk`** 는 공유입니다. `EM_CACHE` 를 돌려쓰십시오.

---

## 4. 무엇을 증명했는지 정확히 말한다

이 저장소의 숫자에는 **세 등급**이 있고 절대 섞지 않습니다:

| 등급 | 뜻 |
|---|---|
| **빌드됨** | 컴파일되고 심볼이 해결된다. 계산 주장이 아니다 |
| **도달함** (reachability) | import 되고 forward 가 끝까지 간다 |
| **일치함** (agreement) | **upstream 의 숫자를 낸다.** 원소 단위 비교 |

`docs/numerics/AGREE.md` 이전의 모든 커버리지 숫자는 **도달함**이었습니다. "돈다"와 "upstream 의 답을
낸다"는 다른 주장이고, **drop-in 이라는 말을 지탱하는 것은 두 번째뿐입니다.**

**허용오차는 고르는 것이 아니라 유도하는 것입니다.** upstream 자신을 float64 로 다시 돌려
그 f32-vs-f64 오차의 p90 을 쓰십시오(8 ulp 바닥). **모델마다 다시 유도하십시오** — BigVGAN
의 자기 오라클 오차는 모집단 상수의 57 배였고, 상수를 그대로 썼다면 **존재하지 않는 48 배
발산**을 보고했을 것입니다.

**가속기에서 "GPU/NPU 에서 돌았다"는 런타임에서 단언해야 합니다.** 답이 맞는 것으로
추론하면 안 됩니다. CoreML 회차는 `MLComputePlan` 의 op 단위 답을 읽어서야 **자기 모델들이
CPU 에서 돌고 있었다**는 것을 알아냈습니다 — 결과는 양쪽 다 맞았습니다. Intel·Qualcomm
스택도 조용히 폴백합니다. `docs/devices/MPSATTN.md` §3.1 은 **소스 스캔 증거를 무력화하는 방법**을
기록해 두었으니, 카운터처럼 소스 서술이 아닌 증거를 쓰십시오.

**측정은 단독으로 돌립니다.** 부하가 걸린 기계의 숫자는 쓸 수 없습니다 — 같은 커밋이
672~1076 ns 를 오간 적이 있습니다. `uptime` 을 먼저 보고, 스위트를 필터링하지 마십시오
(필터링하면 측정 영역 자체가 바뀝니다).

---

## 5. 지시받은 것을 하고 있는지 주기적으로 확인한다

**이 절의 번호는 문서 90 곳에서 인용됩니다. 바꾸지 마십시오.**

개별 단계가 전부 타당한데 총합이 지시에서 멀어지는 일이 반복됐습니다. 아래는 반성문이
아니라 **검사 목록**입니다.

### 5.1 사양은 문서가 아니라 사용자가 준 것이다

사용자가 직접 준 것(스크립트, 노트북, 예시 프로젝트)이 사양입니다. `docs/` 와 ROADMAP 은
**작업의 산출물이지 요구사항이 아닙니다.** 둘이 어긋나면 **사양이 이깁니다.**

작업 시작 전에 사용자가 준 파일을 먼저 찾아 읽으십시오. 없으면 "사양이 어디 있는지"를
물으십시오. 디렉터리 목록으로 답하지 마십시오 — `export/` 에 `coreml.py` 와 `nnapi.py` 밖에
없는 것을 보고 "인텔은 미착수"라고 답한 적이 있는데, 인텔은 `docs/graph/QUANT2.md` §3 에 설계
선례로 기록되어 있었습니다.

### 5.2 사용자가 구조를 말하면 그것이 제약이지 제안이 아니다

설계를 고르기 전에 **사용자가 말한 구조와 내 설계가 양립하는지** 명시적으로 확인하십시오.
"내 방식이 더 낫다"는 판단이 들면 **구현하지 말고 말하십시오.**

### 5.3 테스트 수는 진척이 아니다

보고할 때 **나눕니다**: 기능 추가 / 결함 수정 / 테스트 추가 / 문서 정정 / 삭제.

한 회차가 커널 10 개를 추가하고 **아키텍처 통과 수를 0 에서 0 으로** 옮겼습니다. 다른
회차의 71 개 테스트는 전부 *부재*를 확인하는 것이었고 기능은 하나도 늘지 않았습니다.
**"N 개 추가"가 아니라 "N of M 이 되었다"로 보고하십시오.**

### 5.4 내가 쓴 기준이 답을 정해버린다

감사·조사 프롬프트를 쓸 때 **이 기준으로 찾을 수 없는 것이 무엇인지** 한 줄 적으십시오.
에이전트 결론이 내 가설과 일치하면 **그때 더 의심하십시오.** 내가 기준을 줬기 때문입니다.

능력 기준으로 물으십시오 — "이 파일이 주던 것을 지금은 무엇이 주는가".

커버리지 목록도 이 함정입니다. BigVGAN 이 dispatch 하는 29 개 op 이 전부
`_aten_implemented()` 에 있어서 첫 답은 "빈틈 없음"이었는데, 실제로는 `sum`/`view` 의 **meta
커널**이 없어 모델이 멈췄습니다. 목록은 "커널이 있는가"에 답하고 있었고 질문은 "이 모델이
도는가"였습니다.

### 5.5 실패할 수 없는 검증은 검증이 아니다

**이 저장소에서 가장 많이 인용되는 규칙입니다.** 반복해서 나왔습니다:

- `cargo test` 가 게이트에 없어서 **31 개 테스트를 아무도 돌리지 않았고**, 그중 하나는 자기
  파일의 정렬 단언이 **아무에게도 말하지 않고** 빨간 상태였습니다.
- golden self-test 와 문서 검사기도 같은 이유로 게이트 밖에 있었습니다.
- 골든 케이스 하나가 세 커밋 동안 실패했는데 **마커가 `ge` 하한 둘뿐이라 보이지 않았습니다.**
- 허용오차 가드가 `assert tol == FACTOR * oracle` 이었습니다 — **tol 의 정의를 다시 쓴
  동어반복**이라 배수를 100 배로 늘려도 초록이었습니다.
- 큐 드레인 가드가 **두 곳에 있어 서로를 가려주어**, 어느 쪽도 단독으로 무력화할 수
  없었습니다.
- 21 개 픽스처가 공개 API 대신 날 주소를 써서 **그 API 를 통째로 비워도 통과**했습니다.

**모든 것을 nullify 하십시오.** 일부러 깨고 테스트가 빨개지는지 확인하십시오. **안 잡히는
nullification 은 기능보다 가치 있는 발견입니다** — 반드시 보고하십시오.

**모델 실행을 유일한 검사로 쓰지 마십시오.** 모델은 커널의 일부만 훑습니다 — 한
nullification 이 커널 테스트는 빨갛게 하고 **모델 replay 는 초록으로** 남겼습니다.

유한차분(FD) 오라클의 맹점 셋이 기록되어 있습니다: 실제 모델에서 upstream autograd 와
600 배 어긋나고, 학습 모드 batch-norm 결함을 못 보고, **입력 하나만 미분하므로 weight
gradient 를 아예 보지 않습니다.**

### 5.6 하위 호환은 규칙보다 아래다

규칙과 하위 호환이 부딪히면 **규칙이 먼저입니다.** 깨지는 호출자는 목록으로 만들어
고칩니다. "안 깨지게 하려면 X 를 남겨야 한다"는 생각이 들면, **X 가 금지된 그것인지** 먼저
보십시오.

### 5.7 결론을 실행에 옮기기 전에 보여준다

즉시 요청 범위를 넘는 것(다른 레포, API 시그니처 변경, 파일 삭제, 프로세스 종료, 원격
푸시, **PyPI 업로드**)은 **무엇을 왜 하려는지 말하고 기다립니다.**

조사·측정·보고와, 지시받은 범위 안에서 검증된 작업을 착지시키는 것은 그대로 진행합니다.

**PyPI 업로드는 사용자의 명시적 승인 없이 절대 하지 않습니다.** 토큰은 `~/.pypirc` /
`TWINE_PASSWORD` 에 있고 **읽거나 출력하지 않습니다.**

---

## 6. 거부는 이름을 댄다

지원하지 않는 것은 **자기 이름과 이유를 대며 거부**합니다. 그럴듯한 답을 지어내지
않습니다.

**약속된 거부는 없는 거부보다 나쁩니다.** 릴리스 노트가 네 개의 collective 에 대해 "이름으로
거부한다"고 적어놨는데, 실제로는 거부한 적이 없고 **각 랭크가 자기 입력을 그대로**
돌려주고 있었습니다. 읽는 사람은 확인할 것이 없다고 들은 상태였습니다.

**거부를 약화시켜 더 나아가지 마십시오.** 나아갈 수 없으면 벽의 이름을 보고하십시오.

**§5 의 문장을 재측정하십시오.** 릴리스 노트 §5 의 빈틈 서술 중 **넷이 이미 거짓**이었고,
각각 다른 방식으로 틀렸습니다 — 하나는 쓰여질 때 이미 거짓, 하나는 두 주장이 반대 방향으로
반쪽씩 틀림, 하나는 자기 문단과 모순, 하나는 참이지만 오해를 부름. **재측정 없이 인용하지
마십시오.**

---

## 7. 병렬 실행과 기기

**worktree 는 외장에** — `/Volumes/macMini/worktrees/<이름>`. 내부 SSD 는 여유가 없습니다.
`build/` 와 `target/` 은 쌓이기만 하므로 주기적으로 지웁니다.

**동시 실행 상한**: 빌드·테스트를 도는 에이전트 **3~4** (8 코어 · 16 GB). 읽기·분석만 하는
것은 추가로 3~4.

**병합은 조율 세션에서만.** `git merge` 는 커밋되지 않은 변경이 있으면 **조용히 거부**하므로
`CONFLICT` 만 grep 하면 그 거부를 놓칩니다. 병합 후 **반드시 다시 빌드**하십시오 — 각
브랜치가 따로 통과해도 합친 결과가 깨질 수 있습니다.

병합이 만든 실패의 실례: 한 회차는 자기 base 에서 `zeros_like` 가 거부함을 확인했고, 다른
회차는 병렬 브랜치에서 거기 meta 커널을 넣었습니다. 둘 다 옳았고, 텍스트로는 깨끗이
병합됐고, **동작으로는 거짓인 목록**이 나왔습니다. 그 테스트가 **개수가 아니라 op 이름을
세기 때문에** 잡혔습니다.

### 기기

| | 비고 |
|---|---|
| Android 에뮬레이터 5554 / 5556 | **다른 프로젝트와 공유.** `ANDROID_SERIAL` 필수, `/data/local/tmp/bw_*` 안에서만, 앱 설치 금지 |
| 물리 기기 (Galaxy Tab S9 Ultra, SM8550 / Snapdragon 8 Gen 2, API 36) | Hexagon NPU 있음. 무선 ADB |
| Intel NPU 노트북 (Windows) | 사용자가 직접 돌립니다. 절차를 번호 매겨 제공하십시오 |
| NVIDIA GPU | 사용자 Windows 기계, 또는 Colab (`colab` CLI 로그인 완료) |
| **자기가 시작하지 않은 것은 끄지 마십시오.** 시작한 것은 반드시 끄십시오 | |

`adb` 는 `~/.zshenv` 를 통해 전역입니다. 비대화형 셸이 못 찾으면
`export PATH="$HOME/Library/Android/sdk/platform-tools:$PATH"`.

### 헤드리스 에이전트에게 배경 실행을 시키지 마십시오

`claude -p` 로 띄운 에이전트는 **깨어날 다음 턴이 없습니다.** 오래 걸리는 명령을 배경으로
돌리고 "알림을 기다린다"며 턴을 끝내면 **작업이 통째로 사라집니다.** 프롬프트에 넣으십시오:

> 오래 걸리는 명령은 **포그라운드로** 돌려라. 배경으로 돌리고 기다리지 마라 — 너에게는
> 깨어날 다음 턴이 없다.

---

## 8. 가속기 구조

**앞단은 고정, 뒷단만 교체됩니다.**

```
앞단 (고정)     진짜 transformers · from_pretrained · generate
                └ torchnative 가 잎/서브모듈을 제자리에서 교체
뒷단 (교체)     Apple → CoreML ｜ Android → ExecuTorch/QNN ｜ Windows → Intel
```

`.pte` 나 벤더 블롭은 **모듈 뒤에 숨는 구현 세부**지 사용자가 쥐는 물건이 아닙니다.
`docs/graph/QUANT2.md` §3 이 `quantize_` 를 이 모듈 교체 모양으로 규정합니다. **뒷단 선택은 "어느
것이 벤더 NPU 에 제일 잘 닿나"의 문제일 뿐, API 모양의 문제가 아닙니다.**

NPU 는 op 을 하나씩 받지 않고 **서브그래프를 통째로** 받습니다. 그래서 캡처 층
(`decompose.py` → `refold.py`)이 먼저 있고, 델리게이트들은 그 위의 소비자입니다.

**장치는 두 종류입니다.** candle 이 이미 백엔드를 가진 것(Metal, CUDA)은 `Device` enum 의
variant 라 **`Repr` 팔도, 디스패처 팔도, 우리 커널도 필요 없습니다** — `PyDevice::resolve()`
한 줄입니다. candle 이 안 가진 것(Vulkan)은 셰이더부터 업로드 경로까지 전부 새로 써야
합니다. **`Repr` 팔을 만들기 전에 어느 쪽인지 확인하십시오.**

---

## 9. 알려진 제약

- **`torch.compile` 은 영구 거부**입니다. abi3 가 PEP 523 을 막습니다 (`docs/graph/COMPILE.md` — 다만
  abi3 가 막는 것은 프레임 훅이지 Dynamo 가 아니라는 정정이 §에 있습니다).
- **NNAPI 는 Android 15 에서 deprecated** 됐습니다. 후계는 단일 OS API 가 아니라 **벤더별
  LiteRT delegate** 입니다. `nnapi.py` 는 여전히 동작하므로 지우지 마십시오.
- **`timeout` 명령이 없습니다** (coreutils 미설치). 각 도구의 자체 타임아웃을 쓰십시오.
- **`find` 가 상대 시각(`-newermt '-15 minutes'`)을 에러 없이 빈 결과로** 처리합니다. `stat` 을
  쓰십시오.
- **`cargo` 가 비대화형 PATH 에 없습니다.** `export PATH="$HOME/.cargo/bin:$PATH"`.
- **Docker** 는 `~/.docker/bin/docker` 에 있고 데몬은 **aarch64 리눅스**입니다. manylinux
  aarch64 를 네이티브로 빌드·실행할 수 있습니다. `linux/amd64` 는 QEMU 라 **측정 금지**.
