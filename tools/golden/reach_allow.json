{
  "_README": [
    "Allowlist for tools/golden/reach.py. Every entry is a connection that is",
    "*absent on purpose*; docs/REACH.md is the inventory and the argument.",
    "",
    "Matched exactly, in both directions. An unlisted gap fails the suite, and an",
    "entry whose gap has since closed fails it too -- so closing a gap deletes its",
    "entry in the same change and this file always describes the present.",
    "",
    "`reason` is prose for the reader. `upstream_absent` / `upstream_present` are",
    "for the checker: attribute paths on real upstream torch that the reason is",
    "staking itself on, verified by `reach.py --verify-upstream` (a subprocess",
    "with PYTHONPATH stripped). A reason nobody can put a question to is how",
    "docs/SPELLINGS.md's deliberate gaps would otherwise become indistinguishable",
    "from forgotten ones."
  ],
  "shape1_no_dispatch_arm": {
    "methods:as_strided": {
      "reason": "Listed in methods.json with NO kernel, on purpose, so that `x.as_strided(size, stride)` refuses by the name of the aten overload it resolved to (`aten.as_strided.default`) rather than by 'no matching signature' -- `clamp.Tensor`'s pattern, one level up. The absent thing is not effort but a constructor: `as_strided` is by definition a VIEW over raw strides, and candle 0.11.0 exposes no way to build a Tensor over an existing storage with arbitrary strides. `Layout::new(shape, stride, offset)` is public, but the only public path that reaches it is `Tensor::from_storage`, which takes an OWNED `Storage` and documents 'this uses contiguous strides'; `Tensor { storage: Arc<RwLock<Storage>>, layout }` is private and `same_storage` is `pub(crate)`. So the aliasing the op promises cannot be produced. A materialising gather would return the right values for `longformer`'s read-only `_chunk` (modeling_longformer.py:719) and silently wrong ones for any writer -- the divergence shape docs/VULKAN2.md says must be unrepresentable rather than merely unused, and the one docs/VIEWS.md 6.4 already carries for `slice.Tensor` step>1 and `view.dtype`. Sizing it properly means the same work those two need: a stride-carrying tensor wrapper in this crate, or a candle patch exposing a storage-sharing constructor. docs/TAIL3.md 6. Delete this entry the day either lands.",
      "upstream_present": [
        "torch.Tensor.as_strided",
        "torch.as_strided"
      ]
    }
  },
  "shape1_dead_overload_keys_ceiling": 110,
  "shape2_kernel_without_spelling": {
    "aten._fft_c2c.default": {
      "reason": "Upstream has `torch._fft_c2c` (it is a `_VariableFunctions` member) but no `Tensor._fft_c2c`, and this shim deliberately does NOT put a row in `overloads.json` for it. The reason is not that upstream lacks the door -- it is that the door nobody uses is not the one that reaches this kernel: `torch.fft.rfft`/`fft`/`ifft`/`irfft` are C++-level composites that land on `aten::_fft_c2c` through the dispatcher, and `torch.stft` reaches it through `aten::stft`, which IS spelled and IS exercised. Adding `torch._fft_c2c` would add a spelling with no measured caller, which docs/SPELLINGS.md prices as surface. The kernel is proven element-wise against a live upstream in `pytests/test_fft.py` and, through `aten.stft.*`, by the golden harness. Delete this entry if a caller for the bare spelling is found.",
      "upstream_absent": [
        "torch.Tensor._fft_c2c"
      ]
    },
    "aten._fft_c2r.default": {
      "reason": "Upstream has `torch._fft_c2r` (it is a `_VariableFunctions` member) but no `Tensor._fft_c2r`, and this shim deliberately does NOT put a row in `overloads.json` for it. The reason is not that upstream lacks the door -- it is that the door nobody uses is not the one that reaches this kernel: `torch.fft.rfft`/`fft`/`ifft`/`irfft` are C++-level composites that land on `aten::_fft_c2r` through the dispatcher, and `torch.stft` reaches it through `aten::stft`, which IS spelled and IS exercised. Adding `torch._fft_c2r` would add a spelling with no measured caller, which docs/SPELLINGS.md prices as surface. The kernel is proven element-wise against a live upstream in `pytests/test_fft.py` and, through `aten.stft.*`, by the golden harness. Delete this entry if a caller for the bare spelling is found.",
      "upstream_absent": [
        "torch.Tensor._fft_c2r"
      ]
    },
    "aten._fft_r2c.default": {
      "reason": "Upstream has `torch._fft_r2c` (it is a `_VariableFunctions` member) but no `Tensor._fft_r2c`, and this shim deliberately does NOT put a row in `overloads.json` for it. The reason is not that upstream lacks the door -- it is that the door nobody uses is not the one that reaches this kernel: `torch.fft.rfft`/`fft`/`ifft`/`irfft` are C++-level composites that land on `aten::_fft_r2c` through the dispatcher, and `torch.stft` reaches it through `aten::stft`, which IS spelled and IS exercised. Adding `torch._fft_r2c` would add a spelling with no measured caller, which docs/SPELLINGS.md prices as surface. The kernel is proven element-wise against a live upstream in `pytests/test_fft.py` and, through `aten.stft.*`, by the golden harness. Delete this entry if a caller for the bare spelling is found.",
      "upstream_absent": [
        "torch.Tensor._fft_r2c"
      ]
    },
    "aten.alias.default": {
      "reason": "Upstream exposes no `torch.alias` and no `Tensor.alias` -- `aten::alias` is a dispatcher-internal view op that upstream's own Python surface never names. Inventing a spelling for it would be exactly what docs/SPELLINGS.md refuses (a door upstream does not have). The kernel is reached, and needs to be, through `_aten_dispatch` from traced/exported graphs and through the vendored `__getitem__`'s view path.",
      "upstream_absent": [
        "torch.alias",
        "torch.Tensor.alias"
      ]
    },
    "aten.reflection_pad1d.default": {
      "reason": "Reached upstream as `torch._C._nn.reflection_pad1d` -- and, in practice, as `F.pad(x, pad, mode='reflect')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.reflection_pad1d` nor `Tensor.reflection_pad1d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.reflection_pad1d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='reflect'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.reflection_pad1d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.reflection_pad1d",
        "torch.Tensor.reflection_pad1d"
      ],
      "upstream_present": [
        "torch._C._nn.reflection_pad1d",
        "torch._C._nn.pad"
      ]
    },
    "aten.reflection_pad2d.default": {
      "reason": "Reached upstream as `torch._C._nn.reflection_pad2d` -- and, in practice, as `F.pad(x, pad, mode='reflect')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.reflection_pad2d` nor `Tensor.reflection_pad2d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.reflection_pad2d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='reflect'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.reflection_pad2d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.reflection_pad2d",
        "torch.Tensor.reflection_pad2d"
      ],
      "upstream_present": [
        "torch._C._nn.reflection_pad2d",
        "torch._C._nn.pad"
      ]
    },
    "aten.reflection_pad3d.default": {
      "reason": "Reached upstream as `torch._C._nn.reflection_pad3d` -- and, in practice, as `F.pad(x, pad, mode='reflect')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.reflection_pad3d` nor `Tensor.reflection_pad3d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.reflection_pad3d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='reflect'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.reflection_pad3d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.reflection_pad3d",
        "torch.Tensor.reflection_pad3d"
      ],
      "upstream_present": [
        "torch._C._nn.reflection_pad3d",
        "torch._C._nn.pad"
      ]
    },
    "aten.replication_pad1d.default": {
      "reason": "Reached upstream as `torch._C._nn.replication_pad1d` -- and, in practice, as `F.pad(x, pad, mode='replicate')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.replication_pad1d` nor `Tensor.replication_pad1d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.replication_pad1d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='replicate'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.replication_pad1d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.replication_pad1d",
        "torch.Tensor.replication_pad1d"
      ],
      "upstream_present": [
        "torch._C._nn.replication_pad1d",
        "torch._C._nn.pad"
      ]
    },
    "aten.replication_pad2d.default": {
      "reason": "Reached upstream as `torch._C._nn.replication_pad2d` -- and, in practice, as `F.pad(x, pad, mode='replicate')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.replication_pad2d` nor `Tensor.replication_pad2d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.replication_pad2d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='replicate'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.replication_pad2d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.replication_pad2d",
        "torch.Tensor.replication_pad2d"
      ],
      "upstream_present": [
        "torch._C._nn.replication_pad2d",
        "torch._C._nn.pad"
      ]
    },
    "aten.replication_pad3d.default": {
      "reason": "Reached upstream as `torch._C._nn.replication_pad3d` -- and, in practice, as `F.pad(x, pad, mode='replicate')`, which `torch/nn/functional.py` forwards to `torch._C._nn.pad`, whose C++ binding then picks this op by `mode` and by `len(pad)//2`. Neither `torch.replication_pad3d` nor `Tensor.replication_pad3d` exists on 2.13.0, so this is NOT an overloads.json or methods.json row: a row there would invent a `torch.replication_pad3d` upstream does not have -- the same reasoning `aten.upsample_nearest2d.default` records above. The missing connection is the `mode='replicate'` branch of the `pad` composite in bootstrap.py's `_install_nn`, which today refuses every non-constant mode by name; docs/PAD.md §5 contains the exact patch, already proven against upstream. bootstrap.py was another agent's file this round, so this is a work item, not a design decision, and it fails the suite the moment the branch lands -- which is when this entry should be deleted. The kernel itself is implemented and golden-compared (`aten.replication_pad3d.default`), and it is what moves `torch.stft`'s first wall off the pad (docs/PAD.md §4) and unblocks the `F.pad` half of docs/VOICE.md §1 ranks 12 and 13.",
      "upstream_absent": [
        "torch.replication_pad3d",
        "torch.Tensor.replication_pad3d"
      ],
      "upstream_present": [
        "torch._C._nn.replication_pad3d",
        "torch._C._nn.pad"
      ]
    }
  },
  "shape3_unexercised_spelling": {
    "__add__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__and__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__eq__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__gt__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__imul__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__invert__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__isub__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__le__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__lt__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__matmul__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__mod__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__mul__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__ne__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__neg__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__or__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__radd__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__rmul__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__rsub__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__sub__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "__truediv__": {
      "reason": "operator spelling. Tests exercise it as syntax (`a + b`, `a @ b`, `~a`), which this check reads as text and cannot attribute back to the dunder. A grep for `.__add__(` would be a lie in the other direction."
    },
    "_grouped_mm": {
      "reason": "backlog: the kernel is golden-compared by dispatch key, but no test spells the name. docs/REACH.md §3."
    },
    "multinomial": {
      "reason": "backlog: the kernel is golden-compared by dispatch key, but no test spells the name. Deliberately not attempted in this round: `multinomial` is a random sample, and a test asserting a value against upstream on a random op is exactly the shallow-coverage failure mode this file warns against (a fixed seed narrows the risk but does not remove it, since the shim and upstream draw from different RNG implementations even with the same seed). docs/REACH.md §3."
    }
  }
}
